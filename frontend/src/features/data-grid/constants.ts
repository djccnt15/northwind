export const dataGridInitialState = {
  pagination: {
    paginationModel: { pageSize: 10, page: 0 },
    rowCount: 0,
  },
};

export const defaultColOptions = {
  sortable: false,
  filterable: false,
};
