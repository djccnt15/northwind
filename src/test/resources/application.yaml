spring:
  output.ansi.enabled: always
  profiles.active: test
  jpa:
    # show-sql: true
    properties.hibernate:
      format_sql: true
      highlight_sql: true
      use_sql_comment: true
app:
  defaultPw: 1234
  loginFailureLimit: 6
  remember-me:
    key: northwind-remember-me-key
    tokenValiditySeconds: 1209600  # 14 days
